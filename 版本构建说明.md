# 版本构建说明

本文说明如何使用同一个 `scripts/release.sh` 分别构建 `pro-dev` 的 Pro 版本，以及 `main` 的 main/free 版本。

## 当前规则

`scripts/release.sh` 支持一键参数，也支持按当前 Git 分支自动判断发布通道。

推荐用法：

```bash
bash scripts/release.sh -pro
bash scripts/release.sh -free
```

参数含义：

- `-pro` / `--pro`：构建 Pro 版本，必须在 `pro-dev` 分支运行。
- `-free` / `--free`：构建 main/free 版本，必须在 `main` 分支运行。
- 不带参数：自动判断，`pro-dev` 构建 Pro，其他分支构建 main/free。
- `-auto` / `--auto`：显式使用自动判断。

自动判断规则：

- `pro-dev` 分支：构建 Pro 版本。
- `main` 分支或其他分支：构建 main/free 版本。

也可以用 `RELEASE_CHANNEL` 手动覆盖：

- `RELEASE_CHANNEL=pro`：强制按 Pro 版本构建。
- `RELEASE_CHANNEL=free`：强制按 main/free 版本构建。
- 不设置或设为 `auto`：按当前分支自动判断。

显式参数比环境变量优先。例如即使设置了 `RELEASE_CHANNEL=free`，运行 `bash scripts/release.sh -pro` 仍会按 Pro 构建。

## Pro 版本格式

Pro 版本从 `1.0.1` 开始。

当前 `.buildnumber` 是 `021`，下一次运行 release 脚本会自动递增为 `022`。

在 `pro-dev` 分支运行：

```bash
git checkout pro-dev
git pull

export YTBDOWN_LICENSE_PUBLIC_KEY="$(cat /path/to/token-public-key.pem)"
pnpm preflight:release
bash scripts/release.sh -pro
```

输出格式：

```text
Git tag:        pro-v1.0.1-b022
Release title:  Pro v1.0.1 (Build 022)
macOS DMG:      releases/pro-v1.0.1-b022/YtbDownGUI_Pro_1.0.1_b022_universal.dmg
Windows zip:    YtbDownGUI-Pro-1.0.1-b022-windows-x64.zip
App footer:     YtbDownGUI · Pro v1.0.1 (022)
```

## main/free 版本格式

在 `main` 分支运行同一个脚本：

```bash
git checkout main
git pull

export YTBDOWN_LICENSE_PUBLIC_KEY="$(cat /path/to/token-public-key.pem)"
pnpm preflight:release
bash scripts/release.sh -free
```

输出格式：

```text
Git tag:        v<version>-b<build>
Release title:  v<version> (Build <build>)
macOS DMG:      releases/v<version>-b<build>/YtbDownGUI_<version>_b<build>_universal.dmg
Windows zip:    YtbDownGUI-<version>-b<build>-windows-x64.zip
App footer:     YtbDownGUI · v<version> (<build>)
```

如果在非 `main` 分支上临时构建 main/free 版本：

```bash
export YTBDOWN_LICENSE_PUBLIC_KEY="$(cat /path/to/token-public-key.pem)"
RELEASE_CHANNEL=free pnpm preflight:release
RELEASE_CHANNEL=free bash scripts/release.sh
```

注意：`bash scripts/release.sh -free` 会要求当前分支是 `main`。如果你确实要在非 `main` 分支临时构建 free 包，用上面的 `RELEASE_CHANNEL=free bash scripts/release.sh`。

## 手动强制构建 Pro

如果当前不在 `pro-dev`，但要临时构建 Pro：

```bash
export YTBDOWN_LICENSE_PUBLIC_KEY="$(cat /path/to/token-public-key.pem)"
RELEASE_CHANNEL=pro pnpm preflight:release
RELEASE_CHANNEL=pro bash scripts/release.sh
```

注意：`bash scripts/release.sh -pro` 会要求当前分支是 `pro-dev`。如果你确实要在非 `pro-dev` 分支临时构建 Pro 包，用上面的 `RELEASE_CHANNEL=pro bash scripts/release.sh`。

## 必需环境

本地 macOS release 需要：

```bash
pnpm install
gh auth status
export YTBDOWN_LICENSE_PUBLIC_KEY="$(cat /path/to/token-public-key.pem)"
```

说明：

- `YTBDOWN_LICENSE_PUBLIC_KEY` 来自 License Server 生产环境的 `TOKEN_PUBLIC_KEY`。
- `YTBDOWN_LICENSE_SERVER_URL` 通常不用设置；默认就是 `https://license.ytbdown.litotime.com`。
- `pnpm preflight:release` 会检查版本号一致性、公钥格式、生产授权服务 URL、构建通道、sidecar 二进制和 Tauri `identifier`。
- `scripts/release.sh` 会先跑 preflight，再递增 `.buildnumber`，然后构建、提交 build 号、推送、创建 GitHub Release。

## Windows 构建

Windows 由 GitHub Actions 的 `Release · Windows` workflow 构建。

仓库需要先配置 GitHub Secret：

```text
YTBDOWN_LICENSE_PUBLIC_KEY=<生产 TOKEN_PUBLIC_KEY>
```

触发规则：

- `v*-b*` tag：按 main/free 构建。
- `pro-v*-b*` tag：按 Pro 构建。
- 手动 workflow 如果填写 `tag=pro-v1.0.1-b022`：按 Pro 构建并上传到该 Release。
- 手动 workflow 如果填写 `tag=v1.0.1-b022`：按 main/free 构建并上传到该 Release。

通常不需要单独手动触发 Windows 构建，因为本地 `bash scripts/release.sh` 创建 GitHub Release 后，对应 tag 会自动触发 Windows workflow。

## build 号规则

`.buildnumber` 是每次 release 的递增 build 号。

- 脚本每运行一次，都会把当前值加 1。
- 当前是 `021`，下一次 release 是 `022`。
- 脚本会自动提交 `.buildnumber`，commit message 类似：

```text
chore: bump build number to 022
```

注意：

- 不要为了测试随便运行 `scripts/release.sh`，它会真实递增 build 号、提交并创建 GitHub Release。
- 测试配置只跑 `pnpm preflight:release`。
- Pro 和 main/free 共用同一个 Tauri `productName` 和 `identifier`，不要改成两个 App。

## 版本号规则

版本号需要在三个文件中保持一致：

```text
package.json
src-tauri/tauri.conf.json
src-tauri/Cargo.toml
```

`pnpm preflight:release` 会检查这三个版本是否一致。

当前 `pro-dev` 版本是：

```text
1.0.1
```

main/free 分支按该分支自己的版本号构建；如果需要调整 main/free 版本，也要同步改上面三个文件。
