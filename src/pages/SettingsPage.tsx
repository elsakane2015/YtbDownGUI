export default function SettingsPage() {
  return (
    <div className="page">
      <header className="page-header">
        <h2>设置</h2>
        <p className="muted">下载目录 / 默认画质 / 代理等 — 留到 M5。</p>
      </header>
    </div>
  );
}
